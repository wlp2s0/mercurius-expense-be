import * as queries from "./queries";
import * as mutations from "./mutation";
import * as subscriptions from "./subscription";
const ExpenseQuery = [queries];

const ExpenseMutation = [mutations];

const ExpenseSubscription = [subscriptions];

export { ExpenseQuery, ExpenseMutation, ExpenseSubscription };
